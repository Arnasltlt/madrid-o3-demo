# EEA Air Quality API Configuration
# Copy this file to .env.local and configure as needed

# Contact email for EEA API requests (required by API)
# This email is included in API requests but no authentication is required
EEA_CONTACT_EMAIL=your-email@example.com

# Note: The EEA Air Quality Download Service API does NOT require authentication
# It only requires an email address in the request body for contact purposes

